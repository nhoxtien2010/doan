
<div class="container">
	 <div class="col-sm-6 col-md-4 col-md-offset-4 row">
	   <form class="form-signin">	
			<p><%= t('devise.menu.hello')%> <%= @resource.email %>!</p>

			<p><%= t('devise.menu.account_lock')%></p>
			
			<p><%= t('devise.menu.click_unlock')%></p>
			
			<p><%= link_to t('devise.menu.unlock_account'), unlock_url(@resource, unlock_token: @token) %></p>
		</form>
	</div>
</div>
