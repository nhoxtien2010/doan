<div class="container">
	 <div class="col-sm-6 col-md-4 col-md-offset-4 row">
	   <form class="form-signin">
			<p><%=t('devise.menu.welcome') +  @email.to_s() %>!</p>			
			<p><%= t('devise.menu.confirm_link')%></p>			
			<p><%= link_to t('devise.menu.confirm_account'), confirmation_url(@resource, confirmation_token: @token) %></p>
		</form>
	</div>
</div>
